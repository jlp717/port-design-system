export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-muted-foreground">
        No site built yet. Run <code className="font-mono text-foreground">/clone-website</code> to clone a site
        or <code className="font-mono text-foreground">/port-design-system</code> to port a design system.
      </p>
    </main>
  );
}
